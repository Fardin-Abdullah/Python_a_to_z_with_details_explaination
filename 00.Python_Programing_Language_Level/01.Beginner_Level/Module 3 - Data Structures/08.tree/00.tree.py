# this data structue is like tree it has also root,branch,leaf but root is in top in this case
# child,parent,leaf node first node is root node and second row node is child of first node and which node
# has not any child node are called leaf node
"There can be multiple child of a tree. But in which tree one node can have maximum two child is called "
"binary tree"